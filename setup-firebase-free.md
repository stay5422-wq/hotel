# إعداد Firebase المجاني للمزامنة بين الموظفين

## المشكلة الحالية
- Firebase Credit انتهى
- البيانات محفوظة محلياً فقط (localStorage)
- الموظفين ما يقدروا يشوفوا تحديثات بعض

## الحل: Firebase Spark Plan (مجاني تماماً!)

### الخطوات:

#### 1. إنشاء مشروع Firebase جديد
1. اذهب إلى: https://console.firebase.google.com/
2. اضغط **Add project** (إضافة مشروع)
3. اسم المشروع: `hotel-management-free`
4. **لا تفعّل Google Analytics** (اختر Disable)
5. اضغط **Create project**

#### 2. إعداد Realtime Database
1. من القائمة الجانبية → **Build** → **Realtime Database**
2. اضغط **Create Database**
3. اختر الموقع: **United States (us-central1)**
4. اختار **Start in test mode** (للاختبار)
5. اضغط **Enable**

#### 3. قواعد الأمان (Rules)
في تبويب **Rules**، استبدل القواعد بهذا:

```json
{
  "rules": {
    "hotels": {
      ".read": true,
      ".write": true
    },
    "apartments": {
      ".read": true,
      ".write": true
    },
    "resorts": {
      ".read": true,
      ".write": true
    },
    "caravans": {
      ".read": true,
      ".write": true
    }
  }
}
```

⚠️ **ملاحظة:** هذه القواعد للتطوير فقط. للإنتاج، أضف authentication.

#### 4. الحصول على بيانات المشروع
1. من أيقونة الترس ⚙️ → **Project settings**
2. انزل لـ **Your apps**
3. اضغط على **Web** (</>)
4. اسم التطبيق: `hotel-management`
5. **لا تختار** Firebase Hosting
6. اضغط **Register app**

#### 5. نسخ الإعدادات
ستظهر لك `firebaseConfig` شبيهة بهذه:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "hotel-management-free.firebaseapp.com",
  databaseURL: "https://hotel-management-free-default-rtdb.firebaseio.com",
  projectId: "hotel-management-free",
  storageBucket: "hotel-management-free.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:xxxxxxxxxxxxxxxxxx"
};
```

#### 6. تحديث الملفات المحلية

**افتح كل ملف من هذه:**
- `script.js`
- `apartments.js`
- `resorts.js`
- `caravans.js`

**ابحث عن السطر:**
```javascript
const firebaseConfig = {
```

**استبدل البيانات القديمة** بالبيانات الجديدة من الخطوة 5

⚠️ **مهم:** احتفظ بـ `databaseURL` لأنه الأهم!

#### 7. رفع التحديثات على GitHub

```powershell
cd d:\hotel
git add .
git commit -m "تحديث: ربط Firebase المجاني"
git push origin main
```

#### 8. اختبار النظام

1. **انتظر 2-3 دقائق** حتى يحدث GitHub Pages
2. **افتح الموقع من متصفحين مختلفين:**
   - `https://stay5422-wq.github.io/hotel/hotels.html`
3. **أضف فندق من المتصفح الأول**
4. **حدّث المتصفح الثاني** (F5)
5. **يجب أن يظهر الفندق الجديد!** ✅

---

## حدود Firebase المجاني (Spark Plan)

✅ **مجاني تماماً - بدون بطاقة ائتمان**

**الحدود:**
- **1 GB** تخزين في Realtime Database (كافي جداً لـ100,000+ فندق)
- **10 GB/شهر** تحميل بيانات (كافي لـ10,000+ مستخدم شهرياً)
- **100 اتصال متزامن** (100 موظف في نفس الوقت)

**للمشروع الحالي:**
- 3,000 وحدة إقامة = حوالي **2 MB فقط**
- يكفي لآلاف الموظفين بدون مشاكل! 🎉

---

## البدائل الأخرى (إذا احتجت أكثر)

### 1. Supabase (الأفضل للمشاريع الكبيرة)
- مجاني: **500 MB** قاعدة بيانات
- **Unlimited API requests**
- Realtime subscriptions
- https://supabase.com/

### 2. MongoDB Atlas
- مجاني: **512 MB** تخزين
- Shared cluster
- https://www.mongodb.com/cloud/atlas

---

## الأسئلة الشائعة

**س: هل البيانات محمية؟**
ج: حالياً الوصول مفتوح. للحماية، فعّل Firebase Authentication.

**س: كم عدد الموظفين المسموح؟**
ج: 100 متزامن، وعدد لا محدود يومياً.

**س: ماذا لو انتهت الحدود المجانية؟**
ج: Firebase سيوقف الخدمة مؤقتاً حتى الشهر القادم. لن يخصم منك أي شيء.

**س: كيف أراقب الاستخدام؟**
ج: من Firebase Console → Usage and billing

---

## الدعم

إذا واجهت أي مشكلة:
1. تأكد من `databaseURL` صحيح
2. تأكد من Rules تسمح بالقراءة والكتابة
3. افتح Console (F12) وشوف الأخطاء
